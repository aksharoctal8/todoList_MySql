    <!DOCTYPE html>
    <html lang="en">

    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/css/bootstrap.min.css">
        <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/noty/3.1.4/noty.css">
        <title>Todo List</title>
        <style>
    

            .container {
                width: 500px;
                margin: 50px auto;
                background-color: #fff;
                padding: 20px;
                border-radius: 5px;
                box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
            }

            #todo-form {
                margin-bottom: 20px;
                display: flex;
                flex-direction: column;
                justify-content: center;
            }

            h1 {
                text-align: center;
                color: #333;
            }

            .add-task {
                background-color: #4CAF50;
                color: white;
                padding: 10px 20px;
                border: none;
                border-radius: 5px;
                cursor: pointer;
                margin-bottom: 20px;
            }

            .add-task:hover {
                background-color: #45a049;
            }

            .task-list {
                display: flex;
                justify-content: space-between;
                align-items: center;
                padding: 10px;
                margin-bottom: 10px;
                background-color: #f9f9f9;
                border-radius: 5px;
            }

            .task-list input[type="checkbox"] {
                margin-right: 10px;
            }

            .task-list .task-text {
                flex: 1;
            }

            .task-list .actions {
                display: flex;
            }

            .task-list .actions button {
                background-color: #f44336;
                color: white;
                padding: 5px 10px;
                border: none;
                border-radius: 5px;
                cursor: pointer;
                margin-left: 5px;
            }

            .task-list .actions button:hover {
                background-color: #d32f2f;
            }

            .task-list .actions button:last-child {
                background-color: #2196F3;
            }

            .task-list .actions button:last-child:hover {
                background-color: #1976D2;
            }
        </style>
    </head>

    <div class="container">
        <h1>Update Todo</h1>
    
        <form id="todo-form" action="/todo/editTodo" method="post">
            <input type="hidden" name="EditId" value="<%= up.id %>" >
            <div class="form-group">
                <label for="title">Title:</label>
                <input type="text" class="form-control" id="title" name="todo_title" placeholder="Enter title" value="<%= up.todo_title %>">

            </div>

            <button type="submit" class="btn btn-primary">Edit Todo</button>
            <a href="/todo/view_todo" class="btn btn-success" style="margin-block: 10px;">back</a>
        </form>
        <script src="https://cdnjs.cloudflare.com/ajax/libs/noty/3.1.4/noty.js"></script>   
    </div>
    <% if (locals.flash.success && locals.flash.success.length > 0) { %>		
        <script>
            new Noty({
                type: 'success',
                theme: 'metroui',
                layout: 'topCenter',
                text: '<%= locals.flash.success %>',
                timeout: 1000
            }).show();
        </script>
        <% } %>
        
        <% if (locals.flash.error && locals.flash.error.length > 0) { %>		
        <script>
            new Noty({
                type: 'error',
                theme: 'metroui',
                layout: 'topCenter',
                text: '<%= locals.flash.error %>',
                timeout: 1000
            }).show();
        </script>
        <% } %>
        
    </html>